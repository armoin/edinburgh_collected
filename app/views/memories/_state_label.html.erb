<% if current_user.try(:can_modify?, memory) && show_state? %>
  <div class="state <%= memory.current_state %>">
    <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span> <%= state_label(memory) %>
  </div>
<% end -%>

